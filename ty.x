 program {boolean j int i
     int factorial(int n, int x) {
	if(n < 1) then{
 	return 1}
	else	
        {return factorial(n-1,x) + x}
     }
  while (1==1) {
   i = write(factorial(read(),read()))
     }
  }